import * as React from "react";
import { withRouter } from "react-router";

class AdminProfile extends React.PureComponent {
  render() {
    return <p>AdminProfile</p>;
  }
}

export default withRouter(AdminProfile);
