import * as React from "react";
import { withRouter } from "react-router";

class Petitions extends React.PureComponent {
  render() {
    return <p>Petitions</p>;
  }
}

export default withRouter(Petitions);
